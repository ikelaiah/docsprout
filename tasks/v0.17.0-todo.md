# v0.17.0 task list — Qualification

## Phase 1: Python qualification
- [x] Add the full stable CPython CI matrix (3.10–3.14) and a non-blocking
      3.15 pre-release job; align `requires-python`, classifiers and docs.
- [x] Fix the 3.10/3.11 `tarfile.extractall(filter=)` incompatibility and add
      the regression test.

## Phase 2: Operating-system qualification
- [x] Add Windows and macOS CI at Python 3.10 and 3.14.
- [x] Restructure the Windows symlink test with an unprivileged junction
      fallback; keep the skip narrow and justified if both are impossible.

## Phase 3: Package qualification
- [x] Build wheel and sdist in CI, inspect contents, install into fresh
      environments, and run the installed-package journey from outside the
      repository.
- [x] Add the offline packaging-contract unit module.
- [x] Test wheel and sdist installation.

## Phase 4: Real CLI workflow qualification
- [x] End-to-end subprocess tests for init/check/audit/build/serve/github-pages
      on new and existing projects, with meaningful output assertions.

## Phase 5: Filesystem and repository-shape qualification
- [x] Repository-shape tests: root README, docs index, explicit home, unlisted
      exclude, nested docs, local/nested assets, spaces, Unicode, repository
      path spaces, multi-level navigation, README+docs, ancillary Markdown,
      generic and Pascal shapes.

## Phase 6: Ecosystem qualification
- [x] Non-Git and Git journeys, GitHub Pages generation, idempotent reruns,
      update path, existing/generated configuration, no commit/push rule.

## Phase 7: Generated-site accessibility qualification
- [x] Structural accessibility tests over the visual fixture and DocKit's own
      docs.
- [x] Manual browser/keyboard matrix in docs/qualification.md.

## Phase 8: Qualification documentation
- [x] `docs/qualification.md` written and linked.

## Phase 9: Release metadata and validation
- [x] All v0.17.0 references updated and agreed; roadmap v0.17 expanded.
- [x] Full validation suite green locally; no junk; diff clean.
- [ ] Release branch, PR, merge, tag, GitHub Release, Pages verification.