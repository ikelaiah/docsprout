name: Documentation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  documentation:
    uses: ikelaiah/dockit-fp/.github/workflows/publish-docs.yml@v0.18.0
    with:
      versioned: false
      release: latest
    permissions:
      contents: read
      pages: write
      id-token: write
