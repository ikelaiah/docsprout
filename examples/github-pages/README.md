# GitHub Pages workflow fixture

This is the exact caller workflow generated by `docsprout github-pages` for a
single-version site. It is checked against the deterministic template in the
test suite; users generate it locally rather than copying it by hand.
