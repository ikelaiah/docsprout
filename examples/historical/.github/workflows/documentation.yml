name: Documentation

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  documentation:
    uses: ikelaiah/docsprout/.github/workflows/publish-docs.yml@v1.1.0
    permissions:
      contents: read
      pages: write
      id-token: write
